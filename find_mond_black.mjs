const base=process.env.SUPABASE_URL?.replace(/\/$/,'');const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!base||!key)throw new Error('missing Supabase config');
const params=new URLSearchParams({select:'id,sku,label_display,display_for_packer,name_standard,th_name,alias,unit_price,stock_qty,stock_status,status',or:'(alias.ilike.*ม่อนดำ*,th_name.ilike.*ม่อนดำ*,label_display.ilike.*ม่อนดำ*,display_for_packer.ilike.*ม่อนดำ*)',limit:'20'});
const r=await fetch(`${base}/rest/v1/product_master?${params}`,{headers:{apikey:key,Authorization:`Bearer ${key}`}});const t=await r.text();let j;try{j=JSON.parse(t)}catch{j=null};console.log(JSON.stringify({status:r.status,rows:Array.isArray(j)?j.map(x=>({...x,alias:x.alias?String(x.alias).slice(0,500):x.alias})):[],error:r.ok?null:t.slice(0,400)},null,2));
